version: '3.8'

services:
  api:
    build: .
    ports:
      - "4444:4444"
    volumes:
      - .:/app
      - /app/node_modules
      - sqlite-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  sqlite-data: 